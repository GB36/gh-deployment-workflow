# GitHub Deployment Workflow

This project shows how to automatically deploy an `index.html` file
to GitHub Pages using **GitHub Actions**.

Every time you change `index.html`, the website updates automatically.

Website URL (after deployment):
https://<GB36>.github.io/gh-deployment-workflow/
